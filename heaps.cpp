#include<bits/stdc++.h>

using namespace std;

class heap:
{
    int no_of_heap_elements;
    int current_size;
    int *arr;

    public:
    heap(int max_size);
    heapify(int position);
    int parent(int position)
    int left_child(int position)
    int right_child(int position)
    int remove_max()
};
